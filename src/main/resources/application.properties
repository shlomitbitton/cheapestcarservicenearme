

# ===============================
# = DATA SOURCE
# ===============================

# Set here configurations for the database connection

# Connection url for the database "css"
spring.datasource.url = jdbc:mysql://localhost:3306/ccs?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
# spring.datasource.url = jdbc:mysql://192.185.32.226:3306/shlomit_homeai?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC

# Username and password
spring.datasource.username = root
spring.datasource.password = oiccmmp135
# spring.datasource.username = shlomit_homeai_1
# spring.datasource.password = oiccmmp135

# Keep the connection alive if idle for a long time (needed in production)
# spring.datasource.testWhileIdle = true
# spring.datasource.validationQuery = SELECT 1

server.error.whitelabel.enabled=false

# ===============================
# = JPA / HIBERNATE
# ===============================

# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager).

# Show or not log for each sql query
spring.jpa.show-sql = true

# Hibernate ddl auto (create, create-drop, update): with "update" the database
# schema will be automatically updated accordingly to java entities found in
# the project
# spring.jpa.hibernate.ddl-auto = update
spring.jpa.hibernate.ddl-auto = create

# Naming strategy
# spring.jpa.hibernate.naming-strategy = org.hibernate.cfg.ImprovedNamingStrategy

# Allows Hibernate to generate SQL optimized for a particular DBMS
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.globally_quoted_identifiers=true

spring.servlet.multipart.max-file-size=128KB
spring.servlet.multipart.max-request-size=128KB

# spring.mvc.static-path-pattern=/resources/**
# spring.resources.static-locations=/resources/static/**
# server.port=80
# server.address=192.185.32.226
# spring.mvc.servlet.path=/

# spring.session.store-type=jdbc
# spring.session.jdbc.table-name=SPRING_SESSION
# security.enable.csrf=false

#spring.messages.fallbackToSystemLocale: false

